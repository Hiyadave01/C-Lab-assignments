SUM OF N NATURAL NUMBERS


#include<stdio.h>
int main()
{
    int n, sum = 0;

    printf("Enter a positive integer:");
    scanf("%d", &n);

    if (n<1){s
        printf("Error! Enter a positive integer.");
        return 1;
    }
    
    sum = n * (n + 1) / 2;
    printf("Sum = %d\n", sum);
    return 0;
}